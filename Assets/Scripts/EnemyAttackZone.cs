using System.Collections;
using System.Collections.Generic;
using UnityEngine;
/// Author: Michael Farrar
/// Date: 12/11/24
/// Description: This script is attached to a collider that stops the enemies when they touch it, they will then begin to shoot the player
public class EnemyPerimeter : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    
    private void OnCollisionEnter(Collision collision)
    {
        
    }
}
